# Acuvera Desktop (macOS wrapper)

This is a minimal Tauri wrapper that loads the production web app.

## Quick start

1. Install Tauri prerequisites: https://tauri.app/start/prerequisites/
2. From repo root:
```bash
cd apps/desktop
npm install
npm run tauri dev
```

## Build
```bash
npm run tauri build
```
